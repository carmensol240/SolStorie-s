

## Plan: Add Coloring Pages to Story Packages

### Overview
Add free coloring pages to each story package and display them in the UI, plus grant the credits on purchase.

### 1. `src/config/pricing.ts` — Add `freeColoringPages` field

Update each package in `PRICING_PACKAGES`:
- **basic (3