# דוח בדיקה — סיפור z33c6f (ארי)

מזהה הסיפור שנבדק: `288810d4-893b-4bd3-8767-5996edcb454c` (slug `z33c6f`, נוצר 05/08/2026 17:46 UTC, טווח גיל 0-2, עברית, זכר).
לא בוצע שום תיקון — דיווח בלבד.

## בעיה 2 (מתחילים בקודם, כי היא חד-משמעית): הסיפור לא מתחיל בעמוד 2

**יש 6 שורות מלאות ב-`story_pages`, כולל `page_number = 1`, עם טקסט ועם איור.**
טקסט עמוד 1: "זֶה אָרִי. אָרִי הוּא יֶלֶד אַמִּיץ. הוּא אוֹהֵב אֶת הַיַּעַר…" — כלומר הפתיחה קיימת, ואין שום שגיאה עליה ב-`error_logs` או ב-`illustration_logs`.

הסיבה שהיא לא נראית: הסיפור נוצר בטווח גיל **0-2**, ולכן הצפייה עוברת ל"מצב פעוט" (`isToddler`, `StoryViewer.tsx:1521`) — עמוד אחד משולב לכל עמוד DB. בתוך המצב הזה יש תנאי מפורש (`StoryViewer.tsx:1991`) שמסתיר את הטקסט כאשר `page_number === 1`, כי עמוד 1 משמש גם ככריכה (כותרת "הספר הזה נוצר במיוחד עבורך" + שם הילד).

התוצאה: השקופית הראשונה מציגה איור + כיתוב כריכה + מונה "1 / 6" בלי טקסט, והטקסט הראשון שנקרא הוא של "2 / 6" — "פתאום ארי רואה ארנב קטן…". זה בדיוק מה שראית. הטקסט לא אבד — הוא רק לא מוצג בשקופית הכריכה.

## בעיה 1: הפנים משתנים בעמוד 6

מה שהלוגים כן מראים (`illustration_logs` לסיפור):

| עמוד | מודל | fallback | face ref | משך |
|---|---|---|---|---|
| 1 | gemini_with_face | — | כן | 32.0s |
| 2 | gemini_with_face | — | כן | 25.0s |
| 3 | gemini_with_face | — | כן | 28.0s |
| 4 | gemini_with_face | — | כן | 25.2s |
| 5 | gemini_with_face | — | כן | 34.7s |
| 6 | gemini_with_face | — | כן | 35.2s |

כלומר: **אין fallback** בשום עמוד, כולל 6 — הכול Gemini עם תמונת הפנים.

לגבי `refs=[face:…, page1:…]`: שורות ה-`[IMG-GEN]` של אותה ריצה כבר לא זמינות (חלון שמירת לוגי הפונקציות מחזיק רק כמה דקות אחרונות; הריצה הייתה לפני ~20 דקות). מה שכן ניתן להסיק מהזמנים: עמוד 1 הסתיים ב-17:46:47, וכל העמודים 2-6 התחילו לייצר בין 17:46:49 ל-17:46:50 — כלומר מנגנון ההמתנה `waitForPageOneIllustration` אכן עבד, וכולם (כולל 6) קיבלו את איור עמוד 1 כייחוס. אין הבדל בין עמוד 6 לעמודים 2-5 מבחינת ייחוס.

**מה כן שונה בעמוד 6 — זווית המצלמה:**
המצלמה נבחרת ב-`generate-illustrations/index.ts:1380` לפי `CAMERA_ANGLES[page_number % 6]`, ולכן:

```text
עמוד 1 → wide establishing shot
עמוד 2 → medium shot
עמוד 3 → bird's eye view
עמוד 4 → low angle
עמוד 5 → over-the-shoulder
עמוד 6 → 6 % 6 = 0 → close-up portrait של הפנים ופלג גוף עליון
```

עמוד 6 הוא **העמוד היחיד בסיפור שמקבל קלוז-אפ פנים**. שתי השלכות:
1. בקלוז-אפ הפנים תופסים את רוב הפריים, והמודל מצייר מחדש את כל פרטי הפנים ברזולוציה גבוהה — כל סטייה קטנה מהייחוס נעשית בולטת מיידית, בעוד שבצילומים רחוקים היא נבלעת.
2. יש סתירה בתוך אותו פרומפט: אחרי `CAMERA: close-up portrait…` מגיע בסוף המשפט הקבוע `full body head to toe with feet grounded on surface`. הוראה סותרת כזו דוחפת את המודל לפרש מחדש את הקומפוזיציה, וזה בדיוק המצב שבו מודלים נוטים לסטות גם בזהות.

הלבוש נשאר זהה — תואם לדיווח שלך — כי הוא מוזרק כטקסט מפורש בכל פרומפט (בדקתי: `illustration_prompt` של עמודים 1, 5 ו-6 מכילים את אותה תלבושת מילה במילה).

**עוד ממצא צדדי:** ב-`error_logs` יש רשומה אחת ב-17:48:47 — `illustration_timeout` על `google/gemini-3-pro-image-preview`. היא מאוחרת מכל שישה עמודי הסיפור (שהאחרון בהם נרשם ב-17:47:25) ואינה קשורה לאף אחד מהם; כל 6 העמודים הצליחו מהניסיון הראשון.

## מה אפשר לתקן (לאישורך, לא בוצע)

1. **עמוד 6** — או להוציא את הקלוז-אפ מרשימת הזוויות עבור עמודים עם דמות מזוהה, או לבחור את הזווית כך שהעמוד האחרון לא ייפול על אינדקס 0, וגם להסיר את הסתירה `close-up` מול `full body head to toe`.
2. **עמוד 1 במצב פעוט** — להציג את טקסט עמוד 1 בשקופית נפרדת אחרי הכריכה, או לוותר על הסתרת הטקסט בכריכה, כך שהפתיחה תיקרא.
