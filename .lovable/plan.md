

## Plan: Center Button Text

Single change in `src/pages/Library.tsx` line 425 — add `text-center flex items-center justify-center` to the CTA button className to ensure the Hebrew text is perfectly centered within the button.

