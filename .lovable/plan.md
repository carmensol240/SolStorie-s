

## תיקון תמונות הבאנר של קטגוריות "סקרנות ומדע" ו"עולם הערכים"

### הבעיה
תמונות הדמויות (זואי וסול) נחתכות בבאנרים של הקטגוריות כי הם משתמשים ב-`object-cover` על מסגרת רחבה וצרה (h-36).

### הפתרון
שינוי באנרים ב-`CategorySection.tsx` ו-`TopicStep.tsx` כך שהתמונה תוצג שלמה:

**`src/components/home/CategorySection.tsx`** (שורה 25-29):
- שינוי גובה הבאנר מ-`h-36` ל-`h-44` כדי לתת יותר מרחב לדמות
- שינוי מ-`object-cover` ל-`object-contain` עם `object-right` כדי שהדמות תישאר שלמה בצד ימין
- הוספת רקע gradient כרקע למילוי השטח הריק

**`src/components/wizard/TopicStep.tsx`** (באנר קטגוריה, שורה ~163):
- אותו שינוי: `object-contain` במקום `object-cover` עם מיקום מותאם

### מה לא משתנה
- הגרדיאנט הכהה מעל התמונה (נשאר לקריאות טקסט)
- טקסט הכותרת והאימוג'י
- האינטראקציה (לחיצה לפתיחה/סגירה)

