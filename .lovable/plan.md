

# תיקון שגיאת Syntax ב-ChildInfoStep.tsx

## הבעיה
בשורה 395, בלוק ה-JSX מתחיל ישירות אחרי סגירת הפונקציה `handleDeleteChildProfile` (שורה 393) בלי הצהרת `return (`.

## התיקון
הוספת `return (` בשורה 395, לפני `<div className="w-full space-y-2 px-1">`.

### שינוי יחיד בקובץ `src/components/wizard/ChildInfoStep.tsx`:
- **שורה 395**: שינוי השורה הריקה ל-`return (`

זה יתקן את שגיאות TS1128 בשורות 760-761. לא נדרשים שינויים נוספים -- הלוגיקה של גילאים ואורכי סיפור נשמרת כפי שהיא, וכפתור "הקראה" לא קיים בקובץ.
