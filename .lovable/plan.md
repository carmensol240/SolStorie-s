**מטרה:** להציג מסך חסימת תחזוקה ביצירת סיפורים חדשים בלבד. הספרייה והקריאה ימשיכו לעבוד כרגיל.

**שלבים:**
1. ליצור קומפוננטה חדשה `src/components/MaintenanceBlock.tsx` — מסך מסך מלא (full-screen overlay) עם הטקסט:
   - "🔧 האתר בשיפוצים קלים — נחזור בקרוב! 💜"
   - כפתור "חזרה לספרייה" שמנווט ל-`/library`.
2. לעטוף בקומפוננטה זו את שני המסכים האחראים על יצירת סיפור חדש:
   - `src/pages/CreateStory.tsx` (הראוט `/create`) — להחזיר את `<MaintenanceBlock />` במקום ה-render הרגיל.
   - `src/pages/Adventure.tsx` (הראוטים `/` ו-`/adventure`) — להחזיר את `<MaintenanceBlock />` במקום ה-render הרגיל, כיוון שזו נקודת הכניסה ליצירת סיפור.
3. לא לגעת בשום קובץ אחר — ספרייה (`/library`), צופה הסיפור (`/story/...`), הגדרות, פרופיל וכו' ימשיכו לעבוד.

**הערה:** השינוי הפיך לחלוטין — מספיק להסיר את החסימה משני הקבצים כדי לחזור למצב הרגיל.